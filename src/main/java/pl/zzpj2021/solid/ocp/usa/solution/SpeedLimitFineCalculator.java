package pl.zzpj2021.solid.ocp.usa.solution;

public class SpeedLimitFineCalculator {
    
    public double calculate(State state, int speed) {
        double fine = 0d;
        if (speed > state.getMaxSpeed()) {
            // calculate
        }
        return fine;
    }
    
}
